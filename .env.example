# ===============================================
# DIAL-A-BRAAI ENVIRONMENT VARIABLES
# ===============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# -----------------------------------------------
# FIREBASE CONFIGURATION (Required)
# -----------------------------------------------
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (for server-side operations)
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"

# -----------------------------------------------
# SECURITY
# -----------------------------------------------
# Generate a strong random key for internal API security
# Example: openssl rand -base64 32
INTERNAL_API_KEY=your-secret-internal-api-key-change-this

# Make this available to the client for email dispatch testing
NEXT_PUBLIC_INTERNAL_API_KEY=your-secret-internal-api-key-change-this

# -----------------------------------------------
# AWS SES EMAIL CONFIGURATION (Optional)
# -----------------------------------------------
# Leave these EMPTY for SIMULATION MODE (console logging)
# Fill them in for REAL MODE (actual email sending via AWS SES)

# AWS Credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Email Settings
AWS_SES_FROM_EMAIL=noreply@dialabraai.com

# -----------------------------------------------
# SUPABASE (If using)
# -----------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------
# DEPLOYMENT
# -----------------------------------------------
# For Vercel, AWS Amplify, etc.
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
